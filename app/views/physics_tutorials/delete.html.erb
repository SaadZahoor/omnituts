<div style="margin-top: 10px;">
  <p class="lead" style="margin-bottom: 5px;">Delete: <strong><%=@physicstut.title%></strong></p>
  <%= form_tag({:action => 'destroy', :id => @physicstut.id}) do |f| %>
    <p>Are you sure you want to delete <%= @physicstut.title %> permanently?</p>
    <%= submit_tag("Delete Tutorial", :class => 'btn btn-primary') %>
  <% end %>
  <%= link_to("<< Back to index", {:action => 'index'}) %>
</div>